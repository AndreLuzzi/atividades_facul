#include <stdio.h>

int main() {
    int A, B, C;

    printf("digite tres valores:/n");
    scanf("%d %d %d", &A, &B, &C);
    if (A > B, A > C){
        printf("O maior valor e %d", A);
    } 
    else if (B > A){
        printf("O maior valor e %d", B);
    }
    
    
    return 0;
}